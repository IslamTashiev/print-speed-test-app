import { Box, styled } from "@mui/material";
import React from "react";

const LgoinPage = () => {
	return <LgoinPageWrapper>LgoinPage</LgoinPageWrapper>;
};

const LgoinPageWrapper = styled(Box)(() => ({}));

export default LgoinPage;
